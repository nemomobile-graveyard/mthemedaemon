[Unit]
Description=The MTF theme daemon
After=pre-user-session.target xorg.service
Requires=dbus.socket xorg.service

[Service]
Type=notify
ExecStart=/usr/bin/mthemedaemon -systemd
Restart=always

[Install]
WantedBy=user-session.target 
